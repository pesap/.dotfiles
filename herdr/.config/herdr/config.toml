# Herdr project-session workflow.

[keys]
# Native detach exits the sessionizer; the project shortcut switches only after selection.
detach = "prefix+q"
# Herdr-native indexed workspace navigation: Cmd/Super+1 through Cmd/Super+9.
switch_workspace = "cmd+1..9"

[[keys.command]]
key = "ctrl+backslash"
type = "popup"
command = "herdr-sessionizer --select"
description = "switch Herdr project session"
width = "80%"
height = "70%"

[ui]
agent_panel_sort = "spaces"
